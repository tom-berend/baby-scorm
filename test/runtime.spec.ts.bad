/**
 * @jest-environment jsdom
 */

// if you are going to use DOM, you need the above comment block


import { Runtime } from '../src/runtime/runtime'


beforeEach(() => {
    console.log('before')
});

afterEach(() => {
    console.log('after');
});



describe("Simple expression tests", () => {
    test("fetchJSON", async () => {
        let runtime = new Runtime()
        const fetched = await runtime.fetchJSON('http://localhost/baby/3d/test/testfile.json')

        expect(fetched).toBeTruthy();


    });
})